# 1. import
# 2. load the env variables
# 3. build the hugging face api url
# 4. define a function called get-embedding(which gets the chunk text as property)
#   - takes a chunk  of text
#   - sends it to the hugging face api
#   - returns the vector list (list of numbers)
